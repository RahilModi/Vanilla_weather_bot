{
  "name": "vanila_weather_bot",
  "version": "1.0.0",
  "description": "basic weather bot which is rule based",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rahil Modi",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^1.11.0"
  }
}
